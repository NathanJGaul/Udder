//
//  ContentView.swift
//  Udder
//
//  Created by Nathan Gaul on 1/2/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Adding milk...")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
